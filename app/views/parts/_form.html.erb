<%= form_with(model: part, local: true) do |form| %>
  <% if part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part.errors.count, "error") %> prohibited this part from being saved:</h2>

      <ul>
      <% part.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :part_name %>
    <%= form.text_field :part_name, id: :part_part_name %>
  </div>

  <div class="field">
    <%= form.label :part_number %>
    <%= form.text_field :part_number, id: :part_part_number %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :part_image %>
  </div>

  <div class="field">
    <%= form.label :part_category %>
    <%= form.text_field :part_category, id: :part_part_category %>
  </div>

  <div class="field">
    <%= form.label :part_description %>
    <%= form.text_area :part_description, id: :part_part_description %>
  </div>

  <div class="field">
    <%= form.label :part_purchase_price %>
    <%= form.text_field :part_purchase_price, id: :part_part_purchase_price %>
  </div>

  <div class="field">
    <%= form.label :part_customer_price %>
    <%= form.text_field :part_customer_price, id: :part_part_customer_price %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :part_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
