<%= form_with(model: purchase_order, local: true) do |form| %>
  <% if purchase_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase_order.errors.count, "error") %> prohibited this purchase_order from being saved:</h2>

      <ul>
      <% purchase_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :supplier_id %>
    <%= form.text_field :supplier_id, id: :purchase_order_supplier_id %>
  </div>

  <div class="field">
    <%= form.label :products_list %>
    <%= form.text_area :products_list, id: :purchase_order_products_list %>
  </div>

  <div class="field">
    <%= form.label :eta %>
    <%= form.date_select :eta, id: :purchase_order_eta %>
  </div>

  <div class="field">
    <%= form.label :completed %>
    <%= form.check_box :completed, id: :purchase_order_completed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
